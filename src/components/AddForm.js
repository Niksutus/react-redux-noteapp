import React from 'react';

const AddForm = () => {
  return (
    <div>
      <form>
        Add a new note:
        <input/>
        <button type="submit">Add</button>
      </form>
    </div>
  );
};

export default AddForm;